# 🧪 راهنمای تست مرورگر

## مشکل: صفحه سفید

این مشکل معمولاً به دلایل زیر است:

### 1. Cache مرورگر
**راه‌حل:**
```
1. باز کردن DevTools: F12
2. رفتن به Application/Storage
3. کلیک "Clear site data"
4. Refresh: Ctrl + Shift + R
```

### 2. خطای JavaScript
**بررسی:**
```
1. F12 → Console
2. مشاهده خطاها (اگر وجود دارد)
3. F12 → Network
4. Refresh و بررسی درخواست‌ها
```

### 3. مسیر اشتباه
**بررسی:**
```
آدرس صحیح: http://localhost:5173/admin
نه: http://localhost:5173admin
نه: http://localhost:5173/Admin
```

## ✅ مراحل حل مشکل

### مرحله 1: پاک کردن کامل Cache
```
1. Ctrl + Shift + Delete
2. انتخاب "All time"
3. انتخاب:
   - Cookies
   - Cached images and files
   - Site settings
4. کلیک "Clear data"
5. بستن مرورگر
6. باز کردن مرورگر
```

### مرحله 2: تست با Incognito
```
1. Ctrl + Shift + N (Chrome)
2. رفتن به: http://localhost:5173/admin
3. اگر کار کرد، مشکل از Cache بود
```

### مرحله 3: تست صفحه اصلی
```
1. رفتن به: http://localhost:5173
2. اگر صفحه اصلی کار می‌کند:
   - مشکل فقط در /admin است
3. اگر صفحه اصلی هم سفید است:
   - مشکل کلی‌تر است
```

### مرحله 4: بررسی Console
```
1. F12
2. Console tab
3. اگر خطا دیدید، Screenshot بگیرید
4. خطا را گزارش دهید
```

## 🔍 خطاهای رایج

### خطا: "Failed to fetch"
**معنی:** Backend در دسترس نیست
**راه‌حل:**
```
1. بررسی که Backend اجرا است
2. رفتن به: http://localhost:8080/health
3. باید پیام "OK" ببینید
```

### خطا: "Cannot read property"
**معنی:** خطای JavaScript در کد
**راه‌حل:**
```
1. Screenshot از Console
2. گزارش خطا
```

### خطا: "404 Not Found"
**معنی:** مسیر اشتباه است
**راه‌حل:**
```
1. بررسی URL
2. مطمئن شوید /admin درست تایپ شده
```

## 🎯 تست نهایی

### اگر همچنان صفحه سفید است:

#### تست 1: Backend
```bash
# در مرورگر:
http://localhost:8080/health

# باید ببینید:
{"status":"ok","timestamp":"..."}
```

#### تست 2: Frontend
```bash
# در مرورگر:
http://localhost:5173

# باید صفحه اصلی سایت را ببینید
```

#### تست 3: Admin Login
```bash
# در مرورگر:
http://localhost:5173/admin

# باید صفحه ورود را ببینید
```

## 📸 اسکرین‌شات‌های مورد نیاز

اگر همچنان مشکل دارید، لطفاً اسکرین‌شات بگیرید از:

1. **Console (F12 → Console)**
   - تمام خطاها
   
2. **Network (F12 → Network)**
   - لیست درخواست‌ها
   - درخواست‌های قرمز (failed)
   
3. **Application (F12 → Application)**
   - Local Storage
   - Session Storage

## 🔧 راه‌حل قطعی

اگر هیچ کدام کار نکرد:

```bash
# 1. توقف تمام سرورها
Ctrl + C (در هر دو ترمینال)

# 2. پاک کردن node_modules (اختیاری)
rm -rf node_modules
npm install

# 3. شروع مجدد Backend
cd server
npm start

# 4. شروع مجدد Frontend (ترمینال جدید)
npm run dev

# 5. پاک کردن کامل Cache مرورگر

# 6. تست مجدد
```

## ✅ چک‌لیست نهایی

- [ ] Backend اجرا است (port 8080)
- [ ] Frontend اجرا است (port 5173)
- [ ] Cache مرورگر پاک شد
- [ ] Console خطایی ندارد
- [ ] Network درخواست‌ها موفق هستند
- [ ] URL صحیح است
- [ ] Token معتبر است (اگر لاگین کرده‌اید)

اگر همه موارد بالا OK است اما همچنان صفحه سفید است، لطفاً:
1. Screenshot از Console
2. Screenshot از Network
3. متن دقیق خطا (اگر وجود دارد)

را گزارش دهید.