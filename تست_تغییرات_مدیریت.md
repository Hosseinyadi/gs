# راهنمای تست تغییرات مدیریت آگهی‌ها 🎯

## ✅ سرورها در حال اجرا هستند:
- **Backend**: http://localhost:8080
- **Frontend**: http://localhost:5173

---

## 🔧 تغییرات انجام شده:

### 1. آگهی‌ها دیگر مستقیم منتشر نمی‌شوند ✅
- وقتی کاربر آگهی جدید ایجاد می‌کند، `is_active = 0` تنظیم می‌شود
- پیام به کاربر: "آگهی با موفقیت ایجاد شد و در انتظار تایید مدیریت است"
- آگهی باید توسط مدیر تایید شود تا منتشر شود

### 2. دکمه ویژه کردن درست کار می‌کند ✅
- Endpoint جدید: `POST /api/admin/listings/:id/toggle-featured`
- وضعیت ویژه بودن toggle می‌شود (روشن/خاموش)
- مدت زمان ویژه بودن قابل تنظیم است (پیش‌فرض 30 روز)

### 3. سیستم تایید آگهی‌ها ✅
- دکمه تایید: `POST /api/admin/listings/:id/approve`
- دکمه رد: `POST /api/admin/listings/:id/reject`
- فیلتر آگهی‌های در انتظار: `?status=pending`

---

## 📋 نحوه تست:

### مرحله 1: ایجاد آگهی جدید (به عنوان کاربر)
1. وارد سایت شوید: http://localhost:5173
2. ثبت‌نام یا ورود کنید
3. یک آگهی جدید ایجاد کنید
4. **انتظار**: پیام "آگهی با موفقیت ایجاد شد و در انتظار تایید مدیریت است" را ببینید
5. **بررسی**: آگهی در لیست عمومی نمایش داده نمی‌شود (چون تایید نشده)

### مرحله 2: ورود به پنل مدیریت
1. به آدرس پنل مدیریت بروید: http://localhost:5173/admin/login
2. وارد شوید با:
   - **Username**: `admin`
   - **Password**: `admin123`

### مرحله 3: مشاهده آگهی‌های در انتظار تایید
1. در داشبورد مدیریت، تعداد آگهی‌های در انتظار را ببینید
2. به بخش "مدیریت آگهی‌ها" بروید
3. فیلتر را روی "در انتظار تایید" (pending) بگذارید
4. **انتظار**: لیست آگهی‌های تایید نشده را ببینید

### مرحله 4: تایید آگهی
1. روی یک آگهی کلیک کنید
2. دکمه "تایید" را بزنید
3. **انتظار**: پیام "آگهی تایید شد" را ببینید
4. **بررسی**: آگهی در لیست عمومی سایت نمایش داده می‌شود

### مرحله 5: ویژه کردن آگهی
1. روی یک آگهی تایید شده کلیک کنید
2. دکمه "ویژه کردن" را بزنید
3. مدت زمان را انتخاب کنید (مثلاً 30 روز)
4. **انتظار**: پیام "آگهی ویژه شد" را ببینید
5. **بررسی**: آگهی با علامت "ویژه" در لیست نمایش داده می‌شود
6. **بررسی**: آگهی در بالای لیست قرار می‌گیرد

### مرحله 6: خارج کردن از حالت ویژه
1. روی همان آگهی کلیک کنید
2. دوباره دکمه "ویژه کردن" را بزنید
3. **انتظار**: پیام "آگهی از حالت ویژه خارج شد" را ببینید
4. **بررسی**: علامت ویژه از آگهی حذف می‌شود

### مرحله 7: رد آگهی
1. روی یک آگهی در انتظار کلیک کنید
2. دکمه "رد" را بزنید
3. دلیل رد را وارد کنید (اختیاری)
4. **انتظار**: پیام "آگهی رد شد" را ببینید
5. **بررسی**: آگهی در لیست عمومی نمایش داده نمی‌شود

---

## 🔍 بررسی API ها با Postman یا cURL:

### 1. دریافت آگهی‌های در انتظار:
```bash
curl -X GET "http://localhost:8080/api/admin/listings?status=pending" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
```

### 2. تایید آگهی:
```bash
curl -X POST "http://localhost:8080/api/admin/listings/1/approve" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
```

### 3. ویژه کردن آگهی:
```bash
curl -X POST "http://localhost:8080/api/admin/listings/1/toggle-featured" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"duration_days": 30}'
```

### 4. رد آگهی:
```bash
curl -X POST "http://localhost:8080/api/admin/listings/1/reject" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"reason": "محتوای نامناسب"}'
```

---

## 📊 بررسی داشبورد:

داشبورد مدیریت حالا اطلاعات زیر را نمایش می‌دهد:
- ✅ تعداد کل آگهی‌ها
- ✅ تعداد آگهی‌های فعال
- ✅ تعداد آگهی‌های در انتظار تایید (جدید)
- ✅ تعداد آگهی‌های ویژه (جدید)
- ✅ لیست آگهی‌های در انتظار (جدید)

---

## 🐛 اگر مشکلی پیش آمد:

### مشکل 1: دکمه‌ها کار نمی‌کنند
- **راه حل**: کش مرورگر را پاک کنید (Ctrl + Shift + Delete)
- **راه حل**: صفحه را رفرش کنید (Ctrl + F5)

### مشکل 2: آگهی‌ها نمایش داده نمی‌شوند
- **بررسی**: لاگ سرور را چک کنید
- **بررسی**: Network tab در Developer Tools را باز کنید
- **بررسی**: آیا token مدیریت معتبر است؟

### مشکل 3: خطای 401 Unauthorized
- **راه حل**: دوباره وارد پنل مدیریت شوید
- **راه حل**: بررسی کنید که token در localStorage ذخیره شده باشد

---

## 📝 نکات مهم:

1. **تمام آگهی‌های جدید باید تایید شوند**
2. **فقط مدیران می‌توانند آگهی‌ها را تایید کنند**
3. **دکمه ویژه کردن فقط برای آگهی‌های تایید شده کار می‌کند**
4. **تمام عملیات مدیریتی در جدول audit_logs ثبت می‌شوند**

---

## 🎉 موفق باشید!

اگر همه چیز درست کار کرد، پروژه شما آماده استفاده است!

**تاریخ**: 2024
**وضعیت**: ✅ آماده تست
