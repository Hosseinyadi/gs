# ✅ مدیریت: استان و کارت بانکی رفع شد

## 🔧 تغییرات انجام شده:

### 1. جایگزینی شهر با استان در مدیریت ✅

#### قبل:
```
موقعیت: [ورودی متنی آزاد]
```

#### بعد:
```
استان: [انتخاب از 31 استان ایران]
```

**فایل تغییر یافته:**
- `src/components/admin/AdminListings.tsx`
  - اضافه شدن import ProvinceSelect
  - جایگزینی Input با ProvinceSelect در فرم ویرایش آگهی

---

### 2. رفع مشکل ورودی کارت بانکی ✅

#### مشکل قبلی:
- فقط 8 رقم قبول می‌کرد
- نمی‌شد 16 رقم کامل وارد کرد

#### راه حل:
- اصلاح تابع `handleCardNumberChange`
- اضافه کردن `maxLength={19}` (16 رقم + 3 خط تیره)
- بهبود منطق پاکسازی و فرمت‌بندی

**فایل تغییر یافته:**
- `src/components/admin/AdminPaymentSettings.tsx`

---

## 📋 جزئیات تغییرات:

### AdminListings.tsx

```typescript
// Import اضافه شد:
import ProvinceSelect from "@/components/ui/ProvinceSelect";

// در فرم ویرایش:
<div>
  <label className="text-sm font-medium">استان *</label>
  <ProvinceSelect
    value={editForm.location}
    onValueChange={(value) => setEditForm({ ...editForm, location: value })}
    placeholder="انتخاب استان"
    className="w-full"
  />
</div>
```

### AdminPaymentSettings.tsx

```typescript
// تابع اصلاح شده:
const handleCardNumberChange = (e: React.ChangeEvent<HTMLInputElement>) => {
  const value = e.target.value;
  // حذف تمام کاراکترهای غیر عددی
  const cleaned = value.replace(/\D/g, '');
  // محدود کردن به 16 رقم
  if (cleaned.length <= 16) {
    const formatted = formatCardNumber(cleaned);
    setSettings(prev => ({ ...prev, card_number: formatted }));
  }
};

// Input با maxLength:
<Input
  id="card_number"
  type={showCardNumber ? "text" : "password"}
  value={showCardNumber ? settings.card_number : maskCardNumber(settings.card_number)}
  onChange={handleCardNumberChange}
  placeholder="0000-0000-0000-0000"
  className="pr-10"
  dir="ltr"
  maxLength={19}  // 16 رقم + 3 خط تیره
/>
```

---

## 🧪 تست کنید:

### 1. تست استان در مدیریت:

```
1. وارد پنل ادمین شوید
2. به بخش "مدیریت آگهی‌ها" بروید
3. روی دکمه "ویرایش" یک آگهی کلیک کنید
4. فیلد "استان" را ببینید
   → باید dropdown با 31 استان ایران باشد
5. یک استان انتخاب کنید و ذخیره کنید
   → باید با موفقیت ذخیره شود
```

### 2. تست کارت بانکی:

```
1. وارد پنل ادمین شوید
2. به بخش "تنظیمات پرداخت" بروید
3. در فیلد "شماره کارت" شروع به تایپ کنید:
   
   تایپ: 6037997012345678
   نتیجه: 6037-9970-1234-5678
   
4. باید تمام 16 رقم قبول شود
5. فرمت خودکار با خط تیره اعمال شود
6. ذخیره کنید
   → باید با موفقیت ذخیره شود
```

---

## ✅ نتیجه:

### قبل:
- ❌ شهر: ورودی متنی آزاد (غیر استاندارد)
- ❌ کارت بانکی: فقط 8 رقم قبول می‌کرد

### بعد:
- ✅ استان: انتخاب از 31 استان ایران (استاندارد)
- ✅ کارت بانکی: 16 رقم کامل با فرمت خودکار

---

## 🚀 آماده برای استفاده!

سرورها را restart کنید:
```bash
restart-servers.bat
```

موفق باشید! 🎉
