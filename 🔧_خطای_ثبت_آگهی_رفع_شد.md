# 🔧 خطای ثبت آگهی رفع شد

## ❌ خطای قبلی:
```
اطلاعات آگهی نامعتبر است
```

## 🔍 علت مشکل:

### 1. فیلد `specifications`
- **قبل:** به عنوان object ارسال می‌شد
- **بعد:** به عنوان JSON string ارسال می‌شود

### 2. فیلد `tags`
- **قبل:** به عنوان JSON string ارسال می‌شد
- **بعد:** به عنوان array ارسال می‌شود (backend خودش تبدیل می‌کنه)

### 3. فیلدهای اختیاری
- **قبل:** `undefined` ارسال می‌شد
- **بعد:** مقادیر پیش‌فرض (`''` یا `null`) ارسال می‌شود

---

## ✅ تغییرات انجام شده:

### فایل: `src/pages/PostAd.tsx`

```javascript
// قبل:
specifications: {
  ...JSON.parse(form.specifications || '{}'),
  priceType: form.priceType,
  isNegotiable: form.priceType === 'negotiable'
},
tags: JSON.stringify(form.tags),

// بعد:
specifications: JSON.stringify({
  ...JSON.parse(form.specifications || '{}'),
  priceType: form.priceType,
  isNegotiable: form.priceType === 'negotiable'
}),
tags: form.tags, // array
```

### فیلدهای اختیاری:
```javascript
condition: form.condition || '',
year: form.year ? parseInt(form.year) : null,
brand: form.brand || '',
model: form.model || '',
```

---

## 🧪 تست کنید:

### 1. Restart سرورها:
```bash
restart-servers.bat
```

### 2. لاگین کنید:
```
شماره: 09123456789
کد OTP: 123456
```

### 3. ثبت آگهی:
```
نوع: فروش
عنوان: بیل مکانیکی کوماتسو
دسته‌بندی: بیل مکانیکی
قیمت: 5000000
استان: تهران
توضیحات: بیل مکانیکی کوماتسو مدل PC200 در حد نو
```

### 4. نتیجه مورد انتظار:
```
✅ آگهی با موفقیت ثبت شد و در انتظار تایید مدیر است
```

---

## 📋 ساختار داده ارسالی:

```json
{
  "title": "بیل مکانیکی کوماتسو",
  "description": "بیل مکانیکی کوماتسو مدل PC200 در حد نو",
  "price": 5000000,
  "type": "sale",
  "category_id": 1,
  "location": "تهران",
  "condition": "عالی",
  "year": 2020,
  "brand": "کوماتسو",
  "model": "PC200",
  "specifications": "{\"priceType\":\"fixed\",\"isNegotiable\":false}",
  "tags": ["کوماتسو", "بیل"],
  "images": []
}
```

---

## 🔧 Backend Validation:

Backend این فیلدها را چک می‌کند:
- ✅ `title`: 5-200 کاراکتر
- ✅ `description`: حداقل 10 کاراکتر
- ✅ `price`: عدد مثبت
- ✅ `type`: 'rent' یا 'sale'
- ✅ `category_id`: عدد صحیح
- ✅ `location`: حداقل 2 کاراکتر (استان)

---

## ✅ همه چیز آماده است!

حالا می‌توانید آگهی ثبت کنید بدون خطا! 🚀
