# ✅ سرورها راه افتادند!

## 🎉 وضعیت سرورها:

### Backend ✅
```
✅ در حال اجرا
URL: http://localhost:8080
Status: Running
```

### Frontend ✅
```
✅ در حال اجرا  
URL: http://localhost:5173
Status: Running
```

---

## 🔐 اطلاعات ورود:

### کاربر عادی:
```
URL: http://localhost:5173
شماره: 09123456789
کد OTP: 123456
```

### Master Admin:
```
URL: http://localhost:5173/admin
Username: masteradmin
Password: Master@123456
```

⚠️ **مهم:** بعد از اولین ورود، رمز عبور Master Admin را تغییر دهید!

---

## 🎯 قابلیت‌های آماده:

### 1. ثبت آگهی ✅
- ورود با OTP
- انتخاب نوع (فروش/اجاره)
- آپلود 1-5 عکس
- انتخاب استان (31 استان)
- تگ‌ها (حداکثر 5 تگ)
- تبدیل قیمت به حروف فارسی

### 2. مدیریت ادمین‌ها 👑 ✅
- Master Admin: دسترسی کامل
- Super Admin: دسترسی محدود
- Admin: دسترسی پایه
- مدیریت دسترسی‌ها
- لاگ فعالیت‌ها

### 3. مدیریت آگهی‌ها ✅
- مشاهده و ویرایش
- فعال/غیرفعال کردن
- ارتقا به ویژه
- حذف آگهی‌ها

### 4. Cache Management ✅
- خودکار
- بدون نیاز به پاک کردن دستی

---

## 🚀 شروع کار:

### مرحله 1: باز کردن سایت
```
مرورگر را باز کنید و به آدرس زیر بروید:
http://localhost:5173
```

### مرحله 2: ثبت آگهی
```
1. کلیک روی "ثبت آگهی"
2. ورود با شماره موبایل
3. وارد کردن اطلاعات
4. آپلود عکس‌ها
5. ثبت آگهی
```

### مرحله 3: ورود به پنل ادمین
```
1. به آدرس http://localhost:5173/admin بروید
2. Username: masteradmin
3. Password: Master@123456
4. ورود
```

---

## 📊 API Endpoints:

### Authentication:
- POST `/api/auth/send-otp` - ارسال OTP
- POST `/api/auth/verify-otp` - تایید OTP
- POST `/api/auth/admin/login` - ورود ادمین

### Listings:
- GET `/api/listings` - لیست آگهی‌ها
- POST `/api/listings` - ثبت آگهی
- GET `/api/listings/:id` - جزئیات آگهی
- PUT `/api/listings/:id` - ویرایش آگهی
- DELETE `/api/listings/:id` - حذف آگهی

### Admin Hierarchy:
- GET `/api/admin/hierarchy/admins` - لیست ادمین‌ها
- POST `/api/admin/hierarchy/admins` - ایجاد ادمین
- PUT `/api/admin/hierarchy/admins/:id/permissions` - تغییر دسترسی‌ها
- PATCH `/api/admin/hierarchy/admins/:id/status` - فعال/غیرفعال
- DELETE `/api/admin/hierarchy/admins/:id` - حذف ادمین
- GET `/api/admin/hierarchy/activity-logs` - لاگ فعالیت‌ها

---

## 🔧 در صورت نیاز به Restart:

### توقف سرورها:
```powershell
taskkill /F /IM node.exe
```

### شروع مجدد:
```bash
restart-servers.bat
```

یا به صورت دستی:

```bash
# Terminal 1 - Backend
cd server
node server.js

# Terminal 2 - Frontend
npm run dev
```

---

## 📝 نکات مهم:

1. **Master Admin:**
   - فقط یک Master Admin وجود دارد
   - دسترسی کامل به همه چیز
   - می‌تواند Super Admin بسازد

2. **Super Admin:**
   - دسترسی محدود (قابل تنظیم)
   - می‌تواند Admin عادی بسازد
   - نمی‌تواند Master Admin را ببیند

3. **Admin:**
   - دسترسی پایه
   - فقط مدیریت آگهی‌ها

4. **Cache:**
   - خودکار پاک می‌شود
   - نیازی به Ctrl+Shift+Delete نیست

5. **آگهی‌های جدید:**
   - فوراً نمایش داده می‌شوند
   - بدون نیاز به تایید ادمین

---

## ✅ همه چیز آماده است!

سایت در حال اجراست و آماده استفاده می‌باشد.

**Frontend:** http://localhost:5173  
**Backend:** http://localhost:8080  
**Admin Panel:** http://localhost:5173/admin

موفق باشید! 🚀🎉
