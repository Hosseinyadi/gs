# 📱 راهنمای فعال‌سازی SMS واقعی

## ⚠️ مشکل CORS حل شد!

فایل `server/.env` ایجاد شد و تنظیمات اولیه انجام شد.

---

## 🔑 مراحل فعال‌سازی SMS واقعی:

### گام 1: دریافت API Key از SMS.ir

1. به سایت https://sms.ir بروید
2. وارد پنل کاربری شوید
3. از منوی **"توسعه دهندگان"** → **"API Key"** را کپی کنید

### گام 2: ویرایش فایل `.env`

فایل `server/.env` را باز کنید و این خط را اضافه کنید:

```env
SMS_IR_API_KEY=YOUR_API_KEY_HERE
```

**مثال:**
```env
SMS_IR_API_KEY=AbCdEfGh1234567890IjKlMnOpQrStUvWxYz
```

### گام 3: ریستارت سرور

سرور را متوقف کنید (Ctrl+C) و دوباره اجرا کنید:

```bash
cd server
npm start
```

---

## 📝 محتوای کامل فایل `.env`:

```env
# Runtime
NODE_ENV=development
PORT=8080

# Security
JWT_SECRET=super-secret-jwt-key-for-local-development-12345

# CORS
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://127.0.0.1:24240

# OTP (false = ارسال واقعی SMS)
OTP_MOCK=false

# SMS Provider
SMS_PROVIDER=smsir
SMS_IR_API_KEY=YOUR_API_KEY_HERE
SMS_IR_TEMPLATE_ID=178826
```

---

## ✅ تست با شماره واقعی:

بعد از تنظیم API Key:

1. برو به: http://localhost:5173/auth
2. شماره موبایل واقعی خودت را وارد کن (مثلاً `09123456789`)
3. روی **"ارسال کد تایید"** کلیک کن
4. **کد OTP به شماره موبایلت ارسال می‌شود** 📱
5. کد را وارد کن و تأیید کن
6. ✅ وارد سیستم می‌شوی!

---

## 🔄 برگشت به حالت تست (بدون SMS):

اگر خواستید دوباره بدون ارسال SMS تست کنید:

```env
OTP_MOCK=true
```

در این حالت کد OTP در کنسول سرور نمایش داده می‌شود.

---

## 🐛 عیب‌یابی:

### خطا: "Failed to send OTP"
- مطمئن شوید `SMS_IR_API_KEY` صحیح است
- اعتبار حساب SMS.ir خود را چک کنید
- شماره موبایل باید ایرانی باشد (09xxxxxxxxx)

### خطا: "CORS blocked"
- مطمئن شوید سرور ریستارت شده است
- `ALLOWED_ORIGINS` باید شامل `http://127.0.0.1:24240` باشد

### کد SMS نمی‌رسد
- چند دقیقه صبر کنید (گاهی تاخیر دارد)
- شماره موبایل را بدون فاصله و خط تیره وارد کنید
- مطمئن شوید `SMS_IR_TEMPLATE_ID=178826` است

---

## 📞 پشتیبانی SMS.ir:

اگر مشکلی با ارسال SMS داشتید:
- تماس: 02191004545
- پنل: https://sms.ir
- مستندات: https://docs.sms.ir

---

**✨ حالا می‌توانید با شماره واقعی خودتان ثبت‌نام کنید!**
