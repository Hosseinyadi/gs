# 🚀 سیستم در حال اجرا - آماده تست!

## ✅ وضعیت سرورها

### Backend Server
```
✅ در حال اجرا
🌐 آدرس: http://localhost:8080
📊 Health Check: http://localhost:8080/health
🔄 Cron Jobs: فعال
💾 Database: متصل و آماده
```

### Frontend Server
```
✅ در حال اجرا
🌐 آدرس: http://localhost:5173
⚡ Vite Dev Server
🎨 React + TypeScript
```

---

## 🔐 اطلاعات ورود به پنل ادمین

```
🌐 آدرس ورود: http://localhost:5173/admin/login
👤 نام کاربری: admin
🔑 رمز عبور: admin123456
📧 ایمیل: admin@bilflow.com
```

---

## 📋 مراحل تست

### 1️⃣ ورود به پنل ادمین
```
1. به http://localhost:5173/admin/login بروید
2. نام کاربری: admin
3. رمز عبور: admin123456
4. کلیک روی "ورود"
```

**انتظار**: وارد داشبورد ادمین شوید

---

### 2️⃣ مشاهده پنل مدیریت ادمین‌ها
```
1. بعد از ورود، به آدرس بروید:
   http://localhost:5173/admin/management

2. باید ببینید:
   - لیست ادمین‌ها (1 Super Admin)
   - دکمه "ایجاد ادمین"
   - دکمه "لاگ فعالیت‌ها"
```

**انتظار**: صفحه مدیریت ادمین‌ها نمایش داده شود

---

### 3️⃣ ایجاد ادمین جدید (اختیاری)
```
1. روی "ایجاد ادمین" کلیک کنید
2. اطلاعات را وارد کنید:
   - نام کاربری: moderator1
   - رمز عبور: test123456
   - نام: ناظر اول
   - نقش: Moderator (ناظر)
3. ذخیره کنید
```

**انتظار**: ادمین جدید در لیست نمایش داده شود

---

### 4️⃣ تست ویژه کردن آگهی (مهم! 💰)
```
1. به داشبورد ادمین بروید:
   http://localhost:5173/admin

2. یک آگهی را انتخاب کنید

3. روی دکمه "ویژه کردن" یا "Toggle Featured" کلیک کنید

4. مدت زمان را انتخاب کنید (مثلاً 30 روز)

5. تایید کنید
```

**انتظار**: 
- آگهی ویژه شود (is_featured = 1)
- در جدول featured_listings ثبت شود
- در صفحه اصلی در بالای لیست نمایش داده شود

---

### 5️⃣ بررسی آگهی ویژه در صفحه اصلی
```
1. به صفحه اصلی بروید:
   http://localhost:5173

2. باید ببینید:
   - آگهی‌های ویژه در بالای لیست
   - علامت "ویژه" یا badge روی آگهی
   - آگهی‌های عادی در پایین
```

**انتظار**: آگهی‌های ویژه در بالا نمایش داده شوند

---

### 6️⃣ مشاهده لاگ فعالیت‌ها
```
1. به http://localhost:5173/admin/management بروید

2. روی "لاگ فعالیت‌ها" کلیک کنید

3. باید ببینید:
   - لیست تمام فعالیت‌های انجام شده
   - نام ادمین، عملیات، زمان، IP
```

**انتظار**: لاگ‌های فعالیت نمایش داده شوند

---

### 7️⃣ تست تایید/رد آگهی
```
1. به داشبورد ادمین بروید

2. یک آگهی pending را انتخاب کنید

3. روی "تایید" یا "رد" کلیک کنید
```

**انتظار**: وضعیت آگهی تغییر کند

---

### 8️⃣ مشاهده آنالیتیکس
```
1. به http://localhost:5173/admin/analytics بروید

2. باید ببینید:
   - آمار کلی سیستم
   - نمودارهای فروش
   - آمار آگهی‌ها
```

**انتظار**: صفحه آنالیتیکس نمایش داده شود

---

## 🧪 تست API ها (اختیاری)

### دریافت توکن
```bash
curl -X POST http://localhost:8080/api/auth/admin/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "admin123456"
  }'
```

### لیست ادمین‌ها
```bash
curl -X GET http://localhost:8080/api/admin/management/list \
  -H "Authorization: Bearer {TOKEN}"
```

### ویژه کردن آگهی
```bash
curl -X POST http://localhost:8080/api/admin/listings/1/toggle-featured \
  -H "Authorization: Bearer {TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "duration_days": 30
  }'
```

---

## 📊 بررسی دیتابیس (اختیاری)

### تست سیستم
```bash
node server/test-admin-system.js
```

### بررسی آگهی‌های ویژه
```bash
node -e "const {dbHelpers} = require('./server/config/database'); (async () => { const featured = await dbHelpers.all('SELECT l.id, l.title, l.is_featured, f.end_date FROM listings l LEFT JOIN featured_listings f ON l.id = f.listing_id WHERE l.is_featured = 1'); console.log('Featured Listings:', featured); })();"
```

### بررسی لاگ‌ها
```bash
node -e "const {dbHelpers} = require('./server/config/database'); (async () => { const logs = await dbHelpers.all('SELECT * FROM admin_activity_log ORDER BY created_at DESC LIMIT 10'); console.log('Recent Logs:', logs); })();"
```

---

## ⚠️ مشکلات احتمالی

### مشکل 1: نمی‌توانم وارد شوم
**راه‌حل**:
```bash
# Reset Super Admin
node server/database/create-super-admin.js
```

### مشکل 2: صفحه /admin/management نمایش داده نمی‌شود
**بررسی**:
- آیا Frontend در حال اجراست؟
- آیا route در App.tsx اضافه شده؟
- Console browser را چک کنید

### مشکل 3: ویژه کردن کار نمی‌کند
**بررسی**:
- آیا توکن معتبر است؟
- Network tab را در browser چک کنید
- لاگ‌های backend را ببینید

### مشکل 4: خطای CORS
**راه‌حل**: مطمئن شوید که Backend روی پورت 8080 و Frontend روی 5173 است

---

## 📱 لینک‌های مهم

### Frontend
- 🏠 صفحه اصلی: http://localhost:5173
- 🔐 ورود ادمین: http://localhost:5173/admin/login
- 📊 داشبورد ادمین: http://localhost:5173/admin
- 👥 مدیریت ادمین‌ها: http://localhost:5173/admin/management
- 📈 آنالیتیکس: http://localhost:5173/admin/analytics

### Backend
- 🌐 API Base: http://localhost:8080/api
- 💚 Health Check: http://localhost:8080/health
- 👥 Admin Management: http://localhost:8080/api/admin/management
- ⭐ Toggle Featured: http://localhost:8080/api/admin/listings/:id/toggle-featured

---

---

### 9️⃣ تست سیستم نظرات و امتیازدهی (جدید! ⭐)
```
1. یک آگهی تایید شده انتخاب کنید

2. وارد صفحه جزئیات آگهی شوید

3. در بخش نظرات:
   - آمار امتیازات را مشاهده کنید
   - روی "ثبت نظر" کلیک کنید
   - امتیاز و نظر وارد کنید

4. در پنل ادمین:
   - بخش "نظرات" را انتخاب کنید
   - نظرات را مدیریت کنید
   - پاسخ مدیریت اضافه کنید
```

**انتظار**: 
- نظرات ثبت و نمایش داده شوند
- آمار امتیازات به‌روزرسانی شود
- مدیریت نظرات در پنل ادمین کار کند

---

## ✅ Checklist تست

قبل از تایید نهایی، این موارد را بررسی کنید:

- [ ] می‌توانم وارد پنل ادمین شوم
- [ ] صفحه /admin/management نمایش داده می‌شود
- [ ] می‌توانم ادمین جدید ایجاد کنم
- [ ] می‌توانم آگهی را ویژه کنم (مهم!)
- [ ] آگهی ویژه در صفحه اصلی نمایش داده می‌شود
- [ ] لاگ فعالیت‌ها ثبت می‌شوند
- [ ] تایید/رد آگهی کار می‌کند
- [ ] آنالیتیکس نمایش داده می‌شود
- [ ] **سیستم نظرات کار می‌کند (جدید!)**
- [ ] **امتیازدهی و آمار نمایش داده می‌شود**
- [ ] **مدیریت نظرات در پنل ادمین فعال است**

---

## 🎉 آماده تست!

همه چیز در حال اجراست و آماده تست است!

**نکات مهم**:
1. ✅ Backend: http://localhost:8080
2. ✅ Frontend: http://localhost:5173
3. ✅ Username: admin
4. ✅ Password: admin123456

**اگر مشکلی بود**:
- لاگ‌های console browser را چک کنید
- لاگ‌های backend را ببینید
- اسکریپت تست را اجرا کنید

موفق باشید! 🚀
