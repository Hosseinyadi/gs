# ✅ وضعیت اتصال دیتابیس

## 📊 خروجی سرور:

```
✅ Connected to SQLite database
✅ Database schema initialized
```

## 🔍 معنی:

1. **اتصال موفق**: سرور توانست به فایل دیتابیس `main.db` متصل شود.
2. **ایجاد جداول**: جداول `users` و `listings` ایجاد شدند (یا از قبل وجود داشتند).

## 📂 مسیر دیتابیس:
```
c:\Users\rose\Desktop\site\server\database\main.db
```

## 🛠️ اگر خطا دیدید:

### خطا: `❌ Error connecting to SQLite database`
```
1. پوشه `server/database` وجود دارد؟
2. آیا سیستم می‌تواند فایل `main.db` را ایجاد کند؟
3. دسترسی‌ها را بررسی کنید
```

### خطا: `CREATE TABLE permission denied`
```
1. فایل `main.db` را باز نکرده باشید
2. دسترسی Write در پوشه داشته باشید
```

## 📌 نکات:

- دیتابیس SQLite در یک فایل ذخیره می‌شود
- نیازی به نصب SQL Server نیست
- پشتیبان‌گیری: کپی گرفتن از فایل `main.db`
