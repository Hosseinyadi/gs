# سیستم استان‌ها و تگ‌ها

## ✅ قابلیت‌های پیاده‌سازی شده

### 1. انتخاب استان (Province Selection)

#### ویژگی‌ها:
- ✅ لیست کامل 31 استان ایران
- ✅ گزینه "تمام شهرها" برای نمایش همه آگهی‌ها
- ✅ ذخیره خودکار انتخاب کاربر در localStorage
- ✅ نمایش مودال انتخاب استان در اولین بازدید
- ✅ عدم نمایش مجدد مودال در بازدیدهای بعدی
- ✅ فیلتر خودکار آگهی‌ها بر اساس استان انتخابی

#### کامپوننت‌ها:
- `src/components/ui/ProvinceSelect.tsx` - کامپوننت انتخاب استان
- `src/components/HeroSection.tsx` - مودال انتخاب استان در صفحه اصلی

#### استفاده:
```tsx
import ProvinceSelect from "@/components/ui/ProvinceSelect";

<ProvinceSelect
  value={selectedProvince}
  onValueChange={setSelectedProvince}
  placeholder="انتخاب استان"
  saveToLocalStorage={true}
/>
```

### 2. سیستم تگ‌ها (Tags System)

#### ویژگی‌ها:
- ✅ افزودن تا 5 تگ به هر آگهی
- ✅ پیشنهادات تگ هوشمند
- ✅ نمایش تگ‌ها در کارت آگهی (حداکثر 3 تگ)
- ✅ نمایش کامل تگ‌ها در صفحه جزئیات آگهی
- ✅ امکان حذف تگ با کلیک
- ✅ افزودن تگ با کلید Enter

#### کامپوننت‌ها:
- `src/components/ui/TagInput.tsx` - کامپوننت ورودی تگ

#### استفاده:
```tsx
import TagInput from "@/components/ui/TagInput";

<TagInput
  tags={form.tags}
  onTagsChange={(tags) => setForm(prev => ({ ...prev, tags }))}
  placeholder="مثال: حمل و نقل، اجاره..."
  maxTags={5}
  suggestions={[
    'حمل و نقل',
    'اجاره',
    'فروش',
    'خدمات'
  ]}
/>
```

### 3. نمایش تگ‌ها در آگهی‌ها

#### مکان‌های نمایش:
1. **کارت آگهی** (RentAds.tsx, SaleAds.tsx):
   - نمایش 3 تگ اول
   - استایل Badge با variant="secondary"

2. **صفحه جزئیات** (ListingDetail.tsx):
   - نمایش تمام تگ‌ها
   - در بخش توضیحات آگهی

## 📋 لیست استان‌های ایران

```typescript
const IRANIAN_PROVINCES = [
  'تمام شهرها',
  'آذربایجان شرقی',
  'آذربایجان غربی',
  'اردبیل',
  'اصفهان',
  'البرز',
  'ایلام',
  'بوشهر',
  'تهران',
  'چهارمحال و بختیاری',
  'خراسان جنوبی',
  'خراسان رضوی',
  'خراسان شمالی',
  'خوزستان',
  'زنجان',
  'سمنان',
  'سیستان و بلوچستان',
  'فارس',
  'قزوین',
  'قم',
  'کردستان',
  'کرمان',
  'کرمانشاه',
  'کهگیلویه و بویراحمد',
  'گلستان',
  'گیلان',
  'لرستان',
  'مازندران',
  'مرکزی',
  'هرمزگان',
  'همدان',
  'یزد'
];
```

## 🎯 تگ‌های پیشنهادی

```typescript
const SUGGESTED_TAGS = [
  'حمل و نقل',
  'اجاره',
  'فروش',
  'خدمات',
  'تعمیرات',
  'قطعات یدکی',
  'ماشین‌آلات سنگین',
  'بیل مکانیکی',
  'لودر',
  'کرین'
];
```

## 🔄 فلوی کاربر

### انتخاب استان:
1. کاربر وارد سایت می‌شود
2. اگر استان انتخاب نکرده، مودال نمایش داده می‌شود
3. کاربر استان را انتخاب می‌کند
4. استان در localStorage ذخیره می‌شود
5. آگهی‌ها بر اساس استان فیلتر می‌شوند
6. در بازدیدهای بعدی، مودال نمایش داده نمی‌شود

### ثبت آگهی با تگ:
1. کاربر در صفحه ثبت آگهی، فیلد تگ را می‌بیند
2. می‌تواند از پیشنهادات استفاده کند یا تگ دلخواه بنویسد
3. با Enter تگ اضافه می‌شود
4. حداکثر 5 تگ قابل افزودن است
5. تگ‌ها همراه آگهی ذخیره می‌شوند

### مشاهده آگهی:
1. در لیست آگهی‌ها، 3 تگ اول نمایش داده می‌شود
2. در صفحه جزئیات، تمام تگ‌ها نمایش داده می‌شوند
3. استان آگهی در بخش مشخصات نمایش داده می‌شود

## 💾 ذخیره‌سازی

### localStorage:
```javascript
// ذخیره استان انتخابی
localStorage.setItem('selectedProvince', 'تهران');

// خواندن استان
const savedProvince = localStorage.getItem('selectedProvince');
```

### Database:
```javascript
// تگ‌ها به صورت JSON در فیلد tags ذخیره می‌شوند
tags: JSON.stringify(['حمل و نقل', 'اجاره', 'تهران'])

// استان در specifications ذخیره می‌شود
specifications: {
  province: 'تهران',
  // سایر مشخصات...
}
```

## 🎨 استایل‌ها

### تگ‌ها:
```tsx
// در کارت آگهی
<Badge variant="secondary" className="text-xs">
  {tag}
</Badge>

// در صفحه جزئیات
<Badge variant="outline" className="text-sm">
  {tag}
</Badge>
```

### مودال استان:
- پس‌زمینه: `bg-black/50`
- کارت: `max-w-md` با padding مناسب
- آیکون: `MapPin` با رنگ primary

## 📱 تجربه کاربری

### مزایا:
1. ✅ کاربر فقط یک بار استان را انتخاب می‌کند
2. ✅ آگهی‌های مرتبط با منطقه کاربر نمایش داده می‌شود
3. ✅ تگ‌ها جستجو را آسان‌تر می‌کنند
4. ✅ تگ‌ها اطلاعات بیشتری درباره آگهی ارائه می‌دهند
5. ✅ UI تمیز و کاربرپسند

### نکات:
- کاربر می‌تواند "بعداً انتخاب می‌کنم" را بزند
- کاربر می‌تواند در فیلترها استان را تغییر دهد
- تگ‌ها اختیاری هستند
- حداکثر 5 تگ برای جلوگیری از spam

## 🚀 آماده برای استفاده

تمام قابلیت‌ها پیاده‌سازی شده و آماده تست هستند:

1. ✅ کامپوننت ProvinceSelect
2. ✅ کامپوننت TagInput
3. ✅ مودال انتخاب استان در صفحه اصلی
4. ✅ فیلتر استان در صفحات اجاره و فروش
5. ✅ نمایش تگ‌ها در کارت‌های آگهی
6. ✅ نمایش تگ‌ها در صفحه جزئیات
7. ✅ ذخیره استان در localStorage
8. ✅ ذخیره تگ‌ها در دیتابیس

## 🧪 تست

برای تست:
1. سرورها را راه‌اندازی کنید: `restart-servers.bat`
2. صفحه اصلی را باز کنید
3. مودال انتخاب استان را ببینید
4. استان را انتخاب کنید
5. آگهی جدید ثبت کنید و تگ اضافه کنید
6. تگ‌ها را در لیست و جزئیات آگهی ببینید
