# 🚀 سرورها را ری‌استارت کنید!

## ⚠️ مشکل:
```
خطا در ارتباط با سرور
```

این یعنی سرور backend در حال اجرا نیست.

## ✅ راه‌حل (خیلی ساده):

### روش 1: استفاده از فایل Batch
```bash
restart-servers.bat
```

فقط روی این فایل دوبار کلیک کنید!

### روش 2: دستی
```bash
# Terminal 1 - Backend:
cd server
npm run dev

# Terminal 2 - Frontend:
npm run dev
```

## 🔍 چک کردن سرورها:

### Backend (باید روی port 8080 باشد):
```
http://localhost:8080/api/health
```

اگر کار می‌کند، باید ببینید:
```json
{
  "status": "ok"
}
```

### Frontend (باید روی port 5173 باشد):
```
http://localhost:5173
```

## 📋 چک‌لیست:

- [ ] Terminal backend باز است
- [ ] Terminal frontend باز است
- [ ] Backend روی port 8080 در حال اجرا است
- [ ] Frontend روی port 5173 در حال اجرا است
- [ ] هیچ خطای قرمزی در terminal نیست

## 🎯 بعد از ری‌استارت:

1. صفحه را رفرش کنید (F5)
2. دوباره لاگین کنید (اگر لازم بود)
3. آگهی را ثبت کنید

## 💡 نکته:

اگر سرورها متوقف شدند، احتمالاً یکی از این دلایل است:
- خطای کد
- کرش برنامه
- بستن terminal
- خاموش شدن کامپیوتر

همیشه قبل از کار، مطمئن شوید سرورها در حال اجرا هستند!

## 🚀 الان چه کار کنید:

```bash
# فقط این دستور را اجرا کنید:
restart-servers.bat
```

بعد صفحه را رفرش کنید و دوباره تست کنید! ✅
