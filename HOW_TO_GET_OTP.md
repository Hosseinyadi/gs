# 🔥 راهنمای دریافت کد OTP

## ✅ مشکل حل شد!

**دلیل مشکل**: API Key از SMS.ir تنظیم نشده بود، بنابراین کد ارسال نمی‌شد.

**راه‌حل**: حالت Mock فعال شد تا کد در کنسول سرور نمایش داده شود.

---

## 📱 حالا چطور کد OTP را دریافت کنم؟

### روش 1: مشاهده کد در کنسول سرور (فعلی) ✅

1. **به صفحه ورود برو**: http://localhost:5173/auth
2. **شماره موبایل وارد کن**: مثلاً `09123456789`
3. **روی "ارسال کد تایید" کلیک کن**
4. **به terminal سرور نگاه کن** (همان جایی که `npm start` را اجرا کردی)
5. **کد 6 رقمی را پیدا کن**:
   ```
   [MOCK OTP] to 09123456789: 123456
   ```
6. **کد را در صفحه وارد کن**
7. ✅ وارد می‌شوی!

---

## 📲 روش 2: دریافت SMS واقعی (نیاز به API Key)

اگر می‌خواهید کد به موبایلتان ارسال شود:

### گام 1: دریافت API Key از SMS.ir
1. برو به: https://sms.ir
2. ثبت‌نام کن یا وارد شو
3. از منوی **"توسعه دهندگان"** → **"API Key"** را کپی کن

### گام 2: تنظیم API Key
فایل `server/.env` را باز کن و این خط را اضافه کن:
```env
SMS_IR_API_KEY=your_api_key_here
```

### گام 3: غیرفعال کردن Mock
در همان فایل `.env`:
```env
OTP_MOCK=false
```

### گام 4: ریستارت سرور
```bash
Ctrl + C  (در terminal سرور)
npm start
```

حالا کد به موبایلت ارسال می‌شود! 📱

---

## 🎯 تست سریع (همین الان):

### مرحله 1: باز کردن صفحه
```
http://localhost:5173/auth
```

### مرحله 2: وارد کردن شماره
- شماره: `09123456789` (یا هر شماره دیگری)
- نام: `تست` (اختیاری)

### مرحله 3: ارسال کد
- کلیک روی **"ارسال کد تایید"**

### مرحله 4: پیدا کردن کد
**به terminal سرور نگاه کن** و دنبال این خط بگرد:
```
[MOCK OTP] to 09123456789: 654321
```

### مرحله 5: وارد کردن کد
- کد `654321` را در صفحه وارد کن
- کلیک روی **"تایید"**

### ✅ نتیجه:
وارد سیستم می‌شوی و به پنل کاربری هدایت می‌شوی!

---

## 🔍 کد را کجا ببینم؟

### در Windows Terminal:
پنجره‌ای که دستور `npm start` را در پوشه `server` اجرا کردید را باز کنید.

### در VS Code Terminal:
اگر از VS Code استفاده می‌کنید:
1. پنل Terminal را باز کنید (Ctrl + `)
2. تب مربوط به سرور را انتخاب کنید
3. بعد از کلیک روی "ارسال کد"، کد نمایش داده می‌شود

### مثال خروجی:
```
🚀 Bil Flow Server running on port 8080
Connected to SQLite database
[MOCK OTP] to 09123456789: 123456  ← این کد است!
```

---

## 🐛 عیب‌یابی:

### ❌ کد در کنسول نمایش داده نمی‌شود
- مطمئن شوید سرور ریستارت شده است
- مطمئن شوید `OTP_MOCK=true` در `.env` است
- به صفحه کد می‌رود؟ پس کد ارسال شده، فقط باید در کنسول پیداش کنید

### ❌ "خطا در ارسال پیامک"
- این طبیعی است اگر API Key ندارید
- با `OTP_MOCK=true` کد در کنسول نمایش داده می‌شود

### ❌ "کد نامعتبر است"
- مطمئن شوید کد را درست کپی کرده‌اید
- کد فقط 5 دقیقه معتبر است
- اگر چند بار تلاش کردید، کد جدید بگیرید

---

## 📊 وضعیت فعلی:

```
✅ سرور Backend: در حال اجرا (پورت 8080)
✅ سرور Frontend: در حال اجرا (پورت 5173)
✅ حالت OTP: Mock (کد در کنسول)
❌ SMS واقعی: غیرفعال (نیاز به API Key)
```

---

## 🎉 مراحل بعدی:

1. **همین الان تست کن**: با Mock mode
2. **بعداً SMS واقعی فعال کن**: با API Key از SMS.ir

---

## 💡 نکته مهم:

**حالت Mock برای توسعه و تست عالی است!**
- نیازی به خرید SMS نیست
- سریع‌تر است
- برای تست کامل سیستم کافی است

**SMS واقعی فقط برای production لازم است.**

---

**✨ حالا برو و تست کن! کد را در کنسول سرور خواهی دید.** 🚀
