# 🔐 راهنمای ورود به پنل ادمین

## اطلاعات ورود پیش‌فرض

### ادمین اصلی:
- **نام کاربری**: `hossein`
- **رمز عبور**: `password`

### ادمین دوم:
- **نام کاربری**: `admin`
- **رمز عبور**: `password`

## مراحل ورود

1. به آدرس زیر بروید:
   ```
   http://localhost:5173/admin/login
   ```

2. نام کاربری و رمز عبور را وارد کنید

3. روی دکمه "ورود" کلیک کنید

## مشکلات رایج و راه‌حل‌ها

### ❌ خطای CORS
**علت**: تنظیمات CORS در backend درست نیست

**راه‌حل**:
```bash
cd server
node fix-cors.js
```

سپس سرور را restart کنید.

### ❌ خطای "نام کاربری یا رمز عبور اشتباه است"
**علت**: ممکن است admin user در دیتابیس وجود نداشته باشد

**راه‌حل**:
```bash
cd server
node check-admin.js
```

اگر admin user وجود نداشت، دیتابیس را از نو initialize کنید:
```bash
cd server
node database/init-db.js
```

### ❌ صفحه ادمین لود نمی‌شود
**علت**: ممکن است user های عادی در دیتابیس وجود نداشته باشند

**راه‌حل**:
```bash
cd server
node add-test-user.js
```

## بررسی وضعیت سرورها

### Backend (باید روی پورت 8080 اجرا شود):
```bash
curl http://localhost:8080/health
```

### Frontend (باید روی پورت 5173 اجرا شود):
```
http://localhost:5173
```

## اسکریپت‌های مفید

### بررسی admin users:
```bash
cd server
node check-admin.js
```

### اضافه کردن user تست:
```bash
cd server
node add-test-user.js
```

### اضافه کردن آگهی‌های نمونه:
```bash
cd server
node add-sample-listings.js
```

### رفع مشکل CORS:
```bash
cd server
node fix-cors.js
```

## دسترسی‌های پنل ادمین

پس از ورود موفق، شما به موارد زیر دسترسی دارید:

- 📊 **داشبورد**: آمار کلی سیستم
- 📝 **مدیریت آگهی‌ها**: مشاهده، ویرایش، حذف و تایید آگهی‌ها
- 👥 **مدیریت کاربران**: مشاهده لیست کاربران
- 📈 **آمار و گزارش‌ها**: نمودارها و آمار تفصیلی

## تغییر رمز عبور

برای تغییر رمز عبور admin، از اسکریپت زیر استفاده کنید:

```bash
cd server
node create-test-admin.js
```

یا مستقیماً در دیتابیس:
```sql
UPDATE admin_users 
SET password_hash = '$2a$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi' 
WHERE username = 'hossein';
```

**توجه**: رمز عبور بالا معادل `password` است.

## پشتیبانی

در صورت بروز مشکل:
1. لاگ‌های سرور را بررسی کنید
2. Console مرورگر را چک کنید
3. مطمئن شوید هر دو سرور (frontend و backend) در حال اجرا هستند
