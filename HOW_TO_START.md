# 🚀 چطور پروژه را اجرا کنم؟

## ⚡ روش ساده (توصیه می‌شود):

### 1️⃣ راه‌اندازی سرورها:

**دو کلیک روی این فایل:**
```
start-servers.bat
```

✅ این فایل خودکار:
- Process های قبلی را می‌بندد
- Backend را اجرا می‌کند (port 8080)
- Frontend را اجرا می‌کند (port 5173)
- دو پنجره terminal باز می‌کند

### 2️⃣ متوقف کردن سرورها:

**دو کلیک روی این فایل:**
```
stop-servers.bat
```

✅ این فایل تمام Node.js process ها را می‌بندد

---

## 🌐 لینک‌ها:

بعد از اجرا، این لینک‌ها کار می‌کنند:

```
✅ صفحه اصلی:     http://localhost:5173
✅ پنل مدیریت:     http://localhost:5173/admin/login
✅ Backend API:     http://localhost:8080
```

---

## 🔐 ورود به پنل مدیریت:

```
نام کاربری: admin
رمز عبور: admin123
```

بعد از ورود **13 تب** خواهید دید:
1. داشبورد
2. آگهی‌ها
3. کاربران
4. ارائه‌دهندگان
5. تخفیف‌ها
6. گزارش‌ها
7. رسانه
8. صفحات
9. اعلان‌ها
10. تنظیمات
11. امنیت
12. پشتیبان
13. لاگ‌ها

---

## 🐛 اگر خطا گرفتید:

### ❌ "Port 8080 is already in use"

**راه‌حل:**
1. دو کلیک روی `stop-servers.bat`
2. صبر کنید 2 ثانیه
3. دو کلیک روی `start-servers.bat`

### ❌ "Database not found"

**راه‌حل:**
```bash
cd server
node database/migrate-admin.js
cd ..
```
سپس دوباره `start-servers.bat` را اجرا کنید

---

## 📝 نصب اولیه (فقط بار اول):

اگر اولین بار است پروژه را اجرا می‌کنید:

```bash
# 1. نصب Frontend dependencies
npm install

# 2. نصب Backend dependencies
cd server
npm install
cd ..

# 3. اجرا
start-servers.bat
```

---

## ✅ همین!

**فقط همین! دیگر کاری لازم نیست.**

از این به بعد فقط:
- `start-servers.bat` برای شروع
- `stop-servers.bat` برای توقف

---

**💡 نکته مهم:** همیشه قبل از بستن کامپیوتر، `stop-servers.bat` را اجرا کنید تا Process ها تمیز بسته شوند.
