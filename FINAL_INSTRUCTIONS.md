# ✅ سیستم آماده است - راهنمای نهایی

## 🎉 وضعیت فعلی:

### ✅ سرورها در حال اجرا:
- **Frontend**: http://localhost:5173
- **Backend**: http://localhost:8080
- **مشکل CORS**: حل شد ✅

---

## 📱 ثبت‌نام با شماره واقعی (SMS واقعی)

### ⚠️ مرحله مهم: تنظیم API Key

**فایل `server/.env` را باز کنید و این خط را اضافه کنید:**

```env
SMS_IR_API_KEY=YOUR_API_KEY_HERE
```

### 🔑 دریافت API Key:

1. برو به: https://sms.ir
2. وارد پنل شو
3. منوی **"توسعه دهندگان"** → **"API Key"**
4. کپی کن و در فایل `.env` قرار بده

### 🔄 ریستارت سرور:

بعد از اضافه کردن API Key:

```bash
# در terminal سرور:
Ctrl + C  (برای توقف)
npm start  (برای شروع مجدد)
```

---

## 🧪 تست با شماره واقعی:

### گام 1: باز کردن صفحه ورود
```
http://localhost:5173/auth
```

### گام 2: وارد کردن شماره
- تب **"کاربر"** را انتخاب کن
- شماره موبایل خودت را وارد کن (مثلاً `09123456789`)
- نام خودت را وارد کن (اختیاری)

### گام 3: ارسال کد
- روی **"ارسال کد تایید"** کلیک کن
- **کد OTP به شماره موبایلت ارسال می‌شود** 📱
- ممکن است 1-2 دقیقه طول بکشد

### گام 4: وارد کردن کد
- کد 6 رقمی را که به موبایلت آمد وارد کن
- روی **"تایید"** کلیک کن
- ✅ وارد سیستم می‌شوی!

---

## 🔧 محتوای کامل فایل `.env`:

```env
# Runtime
NODE_ENV=development
PORT=8080

# Security
JWT_SECRET=super-secret-jwt-key-for-local-development-12345

# CORS (مشکل CORS حل شد)
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://127.0.0.1:24240

# OTP Settings
OTP_MOCK=false
OTP_RATE_LIMIT_MAX=10

# SMS Provider
SMS_PROVIDER=smsir
SMS_IR_API_KEY=YOUR_API_KEY_HERE
SMS_IR_TEMPLATE_ID=178826
SMS_IR_VERIFY_URL=https://api.sms.ir/v1/send/verify
```

---

## 🎯 دو حالت کاری:

### حالت 1: SMS واقعی (برای تست با شماره خودت)
```env
OTP_MOCK=false
SMS_IR_API_KEY=your_real_api_key
```
✅ کد به موبایل ارسال می‌شود

### حالت 2: تست بدون SMS (برای توسعه)
```env
OTP_MOCK=true
```
✅ کد در کنسول سرور نمایش داده می‌شود

---

## 🐛 عیب‌یابی:

### ❌ "Failed to fetch"
**حل شد!** CORS تنظیم شد. اگر هنوز مشکل دارید:
- سرور را ریستارت کنید
- مطمئن شوید روی پورت 8080 اجراست

### ❌ "Failed to send OTP"
- مطمئن شوید `SMS_IR_API_KEY` را در `.env` قرار داده‌اید
- API Key باید معتبر باشد
- حساب SMS.ir شما باید اعتبار داشته باشد

### ❌ کد SMS نمی‌رسد
- 1-2 دقیقه صبر کنید
- شماره را بدون فاصله و خط تیره وارد کنید: `09123456789`
- مطمئن شوید شماره ایرانی است
- اعتبار حساب SMS.ir را چک کنید

### ❌ خطای دیتابیس
```bash
cd server
rm database/bilflow.db
npm start
node scripts/create-test-admin.js
```

---

## 📋 چک‌لیست قبل از تست:

- [ ] فایل `server/.env` ایجاد شده
- [ ] `SMS_IR_API_KEY` در `.env` قرار گرفته
- [ ] `OTP_MOCK=false` تنظیم شده
- [ ] سرور ریستارت شده
- [ ] Frontend روی http://localhost:5173 باز است
- [ ] Backend روی http://localhost:8080 در حال اجراست

---

## 🎉 بعد از ورود موفق:

### پنل کاربری: `/dashboard`
- ویرایش پروفایل
- ثبت/ویرایش/حذف آگهی
- مدیریت علاقه‌مندی‌ها
- مشاهده تراکنش‌ها
- تنظیمات حساب

### ورود به عنوان ادمین:
```
نام کاربری: admin
رمز عبور: admin123
```

---

## 📞 پشتیبانی:

### SMS.ir:
- تماس: 02191004545
- سایت: https://sms.ir
- مستندات: https://docs.sms.ir

### مشکلات فنی:
- کنسول مرورگر (F12) را چک کنید
- کنسول سرور را چک کنید
- فایل `SMS_SETUP.md` را مطالعه کنید

---

## 🚀 شروع کنید:

1. **API Key را از SMS.ir دریافت کنید**
2. **در `server/.env` قرار دهید**
3. **سرور را ریستارت کنید**
4. **به http://localhost:5173/auth بروید**
5. **شماره خودتان را وارد کنید**
6. **کد SMS را دریافت و وارد کنید**

---

**✨ موفق باشید! حالا می‌توانید با شماره واقعی خودتان ثبت‌نام کنید.**

📖 فایل‌های راهنما:
- `SMS_SETUP.md` - راهنمای کامل SMS
- `READY_TO_TEST.md` - راهنمای تست
- `ENV_SETUP.md` - راهنمای تنظیمات
