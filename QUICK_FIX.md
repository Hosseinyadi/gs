# 🔧 راهنمای سریع حل مشکل

## ✅ Frontend دوباره راه‌اندازی شد!

سرور Frontend که متوقف شده بود، دوباره اجرا شد.

---

## 🌐 آدرس‌های سرور:

### Frontend (صفحه سایت):
```
http://localhost:5173
```

### Backend (API):
```
http://localhost:8080
```

---

## 🚀 راه‌اندازی سریع (اگر دوباره متوقف شد):

### روش 1: اجرای فایل Batch
دابل کلیک روی:
```
RESTART_SERVERS.bat
```

این فایل:
- ✅ سرورهای قبلی را متوقف می‌کند
- ✅ Backend را اجرا می‌کند
- ✅ Frontend را اجرا می‌کند
- ✅ مرورگر را باز می‌کند

### روش 2: دستی

#### Backend:
```bash
cd server
npm start
```

#### Frontend (در terminal جدید):
```bash
npm run dev
```

---

## 🐛 اگر صفحه باز نشد:

### 1. چک کردن سرورها:
```
Backend:  http://localhost:8080/health
Frontend: http://localhost:5173
```

### 2. پورت اشغال است:
```bash
# متوقف کردن تمام Node.js
taskkill /F /IM node.exe

# شروع مجدد
RESTART_SERVERS.bat
```

### 3. خطای کش مرورگر:
```
Ctrl + Shift + R  (رفرش کامل)
```

### 4. خطای CORS:
- مطمئن شوید Backend روی پورت 8080 اجراست
- فایل `server/.env` را چک کنید

---

## 📊 وضعیت فعلی:

```
✅ Backend Server: در حال اجرا (پورت 8080)
✅ Frontend Server: در حال اجرا (پورت 5173)
✅ SMS واقعی: فعال
✅ نرمال‌سازی شماره: فعال
✅ زمان درخواست مجدد: 90 ثانیه
```

---

## 🎯 مراحل تست:

### 1. باز کردن سایت:
```
http://localhost:5173
```

### 2. رفتن به صفحه ورود:
```
http://localhost:5173/auth
```

### 3. ثبت‌نام:
- شماره: `09123456789`
- کد SMS را دریافت کنید
- وارد شوید

---

## 💡 نکات مهم:

### اگر Frontend متوقف شد:
- معمولاً به دلیل خطای کامپایل است
- Terminal را چک کنید
- یا `RESTART_SERVERS.bat` را اجرا کنید

### اگر Backend متوقف شد:
- معمولاً به دلیل خطای دیتابیس است
- Terminal را چک کنید
- یا `RESTART_SERVERS.bat` را اجرا کنید

### اگر هر دو متوقف شدند:
- `RESTART_SERVERS.bat` را اجرا کنید
- یا دستی هر دو را راه‌اندازی کنید

---

## 🔄 ریستارت سریع:

### همه چیز را از نو شروع کن:
```bash
# 1. متوقف کردن همه
taskkill /F /IM node.exe

# 2. راه‌اندازی مجدد
RESTART_SERVERS.bat
```

---

## 📞 چک کردن سلامت سرورها:

### Backend Health Check:
```
http://localhost:8080/health
```

باید پاسخ دهد:
```json
{
  "status": "ok",
  "timestamp": "..."
}
```

### Frontend:
```
http://localhost:5173
```

باید صفحه اصلی سایت را نمایش دهد.

---

## 🎉 خلاصه:

| مشکل | راه‌حل |
|------|--------|
| Frontend نمایش نمی‌دهد | ✅ دوباره راه‌اندازی شد |
| Backend متوقف شد | `RESTART_SERVERS.bat` |
| هر دو متوقف شدند | `RESTART_SERVERS.bat` |
| خطای CORS | چک کردن `.env` |
| پورت اشغال است | `taskkill /F /IM node.exe` |

---

**✨ حالا سایت باید کار کند!**

🌐 برو به: http://localhost:5173

📖 فایل‌های کمکی:
- `RESTART_SERVERS.bat` - راه‌اندازی سریع
- `OTP_FIXED.md` - راهنمای OTP
- `SMS_ACTIVATED.md` - راهنمای SMS
